
def bubblesort(list):

    for i in range(len()):

        for j in range(0, len(list) - i - 1):

            if list*[j] > node.store[j + 1]:
                temp = [j]
                node.store[j] = node.store[j + 1]
                node.store[j + 1] = temp
    return node.store
